<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

    {% for route in routes %}
        <url>
            <loc>{{ parameters.host }}{{ route.url }}</loc>
            <lastmod>{{ route.updatedAt }}</lastmod>
            <changefreq>monthly</changefreq>
        </url>
    {% endfor %}

</urlset>